name: Export issues
on:
  issues:
    types: [closed]
  workflow_dispatch:
 # schedule:
    # run every 8 hours
  #  - cron:  '0 0,8,16 * * *'
jobs:
  issue:
    runs-on: ubuntu-latest
    name: discord
    steps:
     - name: Test Custom
       uses: rjstone/discord-webhook-notify@v1
       with:
         severity: info
         username: Treasury Guild
         color: '#ff00aa'
         avatarUrl: https://user-images.githubusercontent.com/97610885/177048706-f5046a76-8fa0-4b35-b702-ec48cec1a7a8.png
         description: "Payment Notification"
         details: |
           ${{ fromJSON(github.event.issue.body).project }} 
           https://treasury-guild.gitbook.io/cardano4climate/
           Testing
           Some Markdown text with 😎👀⫸⫷
         footer: "View transaction in Gitbook link"
         text: ${{ github.event.issue.title }}
         webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
